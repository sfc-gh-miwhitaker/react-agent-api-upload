# ==============================================================================
# Snowflake Cortex Agent Chat Application - Environment Configuration
# ==============================================================================
#
# QUICK START:
#   1. Copy this file: cp .env.example .env
#   2. Choose authentication method (password OR key-pair)
#   3. Fill in your Snowflake account details
#   4. Run: ./tools/02_run_app.sh
#
# SECURITY: The .env file is gitignored - NEVER commit credentials!
#
# ==============================================================================

# ==============================================================================
# Authentication Method (choose ONE)
# ==============================================================================

# Option A: Password Authentication (quick start, less secure)
# Uncomment these lines to use password authentication:
# SNOWFLAKE_AUTH_TYPE=password
# SNOWFLAKE_USER=your_username
# SNOWFLAKE_PASSWORD=your_password

# Option B: Key-Pair Authentication (RECOMMENDED for production)
# 1. Generate keys: ./tools/01_setup_keypair_auth.sh --account YOUR_ACCOUNT
# 2. Run the SQL output in Snowsight
# 3. Uncomment these lines:
SNOWFLAKE_AUTH_TYPE=keypair
SNOWFLAKE_USER=SFE_REACT_AGENT_USER
SNOWFLAKE_PRIVATE_KEY_PATH=/absolute/path/to/config/keys/rsa_key.p8

# ==============================================================================
# Snowflake Account Configuration (required for both auth methods)
# ==============================================================================

SNOWFLAKE_ACCOUNT=your_account_identifier
SNOWFLAKE_ROLE=SFE_REACT_AGENT_ROLE
SNOWFLAKE_WAREHOUSE=SFE_REACT_AGENT_WH
SNOWFLAKE_DATABASE=SNOWFLAKE_EXAMPLE
SNOWFLAKE_SCHEMA=REACT_AGENT_STAGE

# Optional: Region (only if not using standard account identifier format)
# SNOWFLAKE_REGION=us-west-2

# ==============================================================================
# Cortex Agent Configuration
# ==============================================================================

SNOWFLAKE_AGENT_NAME=DoctorChris
SNOWFLAKE_STAGE=DOCUMENTS_STAGE

# ==============================================================================
# Server Configuration
# ==============================================================================

PORT=3001
NODE_ENV=development

# ==============================================================================
# Optional: Logging
# ==============================================================================

LOG_LEVEL=info

# ==============================================================================
# NOTES
# ==============================================================================
#
# Authentication Methods:
# -----------------------
# Password Auth:
#   ✓ Quick setup (30 seconds)
#   ✗ Less secure
#   ✗ Passwords can be compromised
#   ✗ No key rotation
#
# Key-Pair Auth (Recommended):
#   ✓ More secure (cryptographic keys)
#   ✓ No password exposure
#   ✓ Better for automation
#   ✓ Supports key rotation
#   ✓ Automated setup with tools/03_setup_keypair_auth.sh
#
# For detailed setup instructions, see:
#   - docs/01-KEYPAIR-AUTH.md
#   - README.md (Security Considerations section)
#
